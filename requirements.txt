firebase-admin>=6.0.0
python-dateutil>=2.8.2 

# Python Stock Service Requirements
# This file contains dependencies for a Python microservice that uses yfinance
# to fetch real-time stock market data
#
# To get the latest versions, run:
# pip install --dry-run --report - yfinance flask flask-cors requests pandas numpy aiohttp python-dotenv redis sqlalchemy structlog pytest pytest-asyncio black flake8 gunicorn
# Or simply use 'pip install -r requirements.txt' and pip will resolve to latest compatible versions

# Core dependencies - using compatible release to allow patch updates
yfinance~=0.2.63
flask~=3.1.1
flask-cors~=6.0.1
flask-socketio~=5.4.1
python-socketio~=5.11.0
requests~=2.32.4

# Data processing - pandas 2.x and numpy 2.x are stable
pandas~=2.3.0
numpy~=2.3.0

# Async support
aiohttp~=3.12.13

# Environment and configuration
python-dotenv~=1.1.0

# Database (optional, for caching)
redis~=6.2.0
sqlalchemy~=2.0.41

# Logging and monitoring
structlog~=25.4.0

# Testing
pytest~=8.4.1
pytest-asyncio~=1.0.0

# Development
black~=25.1.0
flake8~=7.3.0
ruff>=0.3.0  # Modern, fast linter alternative to flake8

# Production server
gunicorn~=23.0.0

# Additional financial data sources (alternatives to yfinance)
# alpha-vantage~=2.3.0
# finnhub-python~=2.4.0
# quandl~=3.7.0
